# Shared with demo for local account cookies
JUDE_SESSION_SECRET=

# Local dev: npm run dev:marketing → http://localhost:3001
# Run `npm run setup:local` from repo root to generate .env.local for both apps.
